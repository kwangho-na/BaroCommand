print("=========== Baro Script ============");

inc('common.comm');
inc('common.dev');
inc('common.etc');
inc('common.class');
inc('common.util');
inc('common.my');

call( conf('src.commonFunc') );
page=pageLoad('dev.tool').commandTool();
not( typeof(page,'page') ) {
	print(@error, "시작페이지 오류");
}
Cf[mainPage]=page;
